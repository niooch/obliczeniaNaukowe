#Jakub Kogut
using Printf
#zwroć wartość
for T in (Float16, Float32, Float64)
    @printf("wartość wyrażenia 3(4/3 - 1) - 1 wynosi: %.20e\n", T(3) * (T(4) / T(3) - T(1)) - T(1))
    @printf("macheps wynosi:                           %.20e\n\n", eps(T))
end
